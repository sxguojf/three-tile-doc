# vue 中使用

---

使用 ref 取得 dom 作为地图容器，其它代码不变

<demo vue="demo/08.vue" title="vue演示" description="鼠标左键拖动地图，右键旋转地图，滚轮缩放"/>

:::tip
在 threejs 的渲染循环中操作 vue 响应式变量并绑定显示，会降低 threejs 的渲染速度，比如在循环中获取操作响应式变量显示摄像机、控制器、地图位置等状态。
:::
